# Marketing-OS — Full System Audit (UX/UI · DB · RLS · E2E)

วันที่ตรวจ **1 ส.ค. 2569** · โค้ดที่ตรวจ = `origin/main` @ `bd0c3c8` (PR #140) · DB = Supabase `zxxpyknoymdlhckpchse`

> **หมายเหตุขอบเขต** — โฟลเดอร์ `Downloads/Marketing-OS` ตามหลัง `origin/main` อยู่ **125 commits** (อยู่บน branch `feat/platform-performance-sheet-mirror` + 2 commit ที่ยังไม่ push) ผมจึงตรวจจาก worktree ที่ checkout `origin/main` ตรง ๆ ไม่แตะ working tree ของคุณ

---

## A. Executive Summary

| รายการ | จำนวน |
|---|---|
| Page route ที่ตรวจ | 26 (+1 หน้า 404) |
| API route ที่ตรวจ | 12 |
| Module ที่ตรวจตามลิสต์ 26 ข้อ | 26 (พบจริง 20 · PARTIAL 3 · NOT FOUND 3) |
| Table ที่ตรวจ | 35 (public schema ทั้งหมด) |
| RLS policy ที่ตรวจ | 71 |
| Function / Trigger ที่ตรวจ | 15 function · 8 trigger |
| Storage bucket | **0** (ระบบไม่ได้ใช้ Supabase Storage เลย) |
| Test ที่รัน | 1,673 unit/logic · 61 RLS probe (positive+negative) · 27 HTTP route probe · dead-link sweep · browser console/network sweep |
| Passed | 1,673 / 1,673 unit · lint ผ่าน · typecheck ผ่าน · production build ผ่าน (30 หน้า) |
| Failed | 0 (ฝั่ง test suite) |
| Bug ที่พบ | **P0 = 0 · P1 = 1 · P2 = 5 · P3 = 6** |
| แก้แล้ว | **1 (P1) — apply บน production แล้ว 1 ส.ค. 2569 06:01 UTC และ verify ผ่าน** |
| ยังเหลือ | P2 × 5 · P3 × 6 |

### Production Ready: **YES** (เหลือ 1 toggle)

`security_p14_brand_scope_work.sql` ถูก apply บน production แล้ว (1 ส.ค. 2569 06:01 UTC · มีแถวใน `schema_migrations`) และ verify ซ้ำผ่านครบ — release gate ผ่านทุกข้อ: ไม่มี P0/P1 · RLS ครบทุก exposed table · negative test ผ่าน · ไม่มี secret ใน client · typecheck/lint/1,673 test/build ผ่าน · ไม่มี console error · ไม่มี orphan/duplicate

**เหลืออีก 1 อย่างที่ควรทำก่อนถือว่าปิดจบ:** เปิด Leaked Password Protection (Supabase Dashboard → Authentication → Password)

ข้อยกเว้นที่ยังไม่ผ่านตามบรีฟ: gate ข้อ "ต้องมี E2E critical flow test" — โปรเจกต์ยังไม่มี Playwright (ดู Remaining Risks ข้อ 8)

---

## B. System Connection Map

```
Browser (Next.js 14 App Router, client components ล้วน)
  │
  ├─ AuthProvider (lib/auth.tsx)
  │     NEXT_PUBLIC_REQUIRE_AUTH=true + Supabase configured → บังคับ login
  │     resolve members row → Role (UI gating เท่านั้น)
  │     fail-safe: ไม่มี member row / ยังโหลดไม่เสร็จ = "Content Creator" (ต่ำสุด) ไม่ใช่ CMO
  │
  ├─ supabase() anon client  ──► PostgREST ──► RLS (แหล่งบังคับสิทธิ์ตัวจริง)
  │     JWT claims: app_role (admin|staff|agency|none), member_role
  │     ตั้งโดย custom_access_token_hook (อ่าน members.access / brand_access / role)
  │
  └─ fetch("/api/*") + Authorization: Bearer <access_token>
        │
        ├─ requireApiUser()      → 401 ถ้าไม่มี/ไม่ valid    [ครบทุก route ยกเว้น /api/health]
        ├─ requireModuleLevel()  → 403 ตาม permissions matrix [ใช้ที่ /api/meta/publish]
        ├─ CRON_SECRET           → 401/503                     [/api/notify/digest]
        └─ supabaseAdmin() (service role, import "server-only") → bypass RLS

Data flow ของ workflow หลัก
  campaigns(id TEXT "CAM-…")
    ├─► content_posts.campaign_id  (FK)  ──► caption / asset
    ├─► graphic_requests.campaign_id (FK) ──► assets.graphic_request_id (UNIQUE)
    ├─► kols.campaign_id (FK)
    ├─► tasks.campaign_id (FK)
    ├─► expense_requests.campaign_id (FK) ──► expenses
    └─► campaign_results.campaign_id (FK, ON DELETE CASCADE)

Outbound
  Google Sheet mirror (Apps Script webhook) · LINE Messaging API · Resend email · Slack bot · Meta publish
```

---

## C. Issue Table

| ID | Sev | Module | ปัญหา | Root Cause | ความเสี่ยง | ไฟล์ / ตาราง | วิธีแก้ | หลักฐาน | สถานะ |
|---|---|---|---|---|---|---|---|---|---|
| **P1-01** | **P1** | Task / KOL / Asset | สมาชิกที่ถูกจำกัดแบรนด์ **อ่าน แก้ และลบ** ข้อมูลของแบรนด์นอกสิทธิ์ได้ | `security_p9_brand_scope.sql` ใส่ `brand_visible()` ให้ 7 ตาราง แต่ **ตกหล่น `tasks`, `kols`, `assets`** ทั้งที่ทั้งสามมีคอลัมน์ `brand` + FK ไป `brands` — ตอนเขียน p9 สมาชิกทุกคนยังเป็น "All brands" ช่องจึงยังไม่มีผล วันนี้มี 2 คนเป็น "Selected brands · Teppen · Mainichi" | ข้อมูลงาน/ค่าตัว KOL/ลิงก์ไฟล์ของ Omakase หลุดถึงคนนอกแบรนด์ และ **ลบทิ้งได้จริง** | `tasks`, `kols`, `assets` | `supabase/security_p14_brand_scope_work.sql` | probe ในนาม `orapan.ch@` อ่าน omakase tasks ได้ **64** · kols **12** · assets **1** · `UPDATE` สำเร็จ **rows=64** · `DELETE kols` สำเร็จ **rows=12** | **✅ FIXED — apply prod แล้ว · verify ผ่าน** |
| P2-01 | P2 | Team KPI | หน้า `/performance-center/team-kpi` ขึ้น "โหมดอ่านอย่างเดียว" ให้ non-CMO แต่คนกลุ่มนั้น **อ่านไม่เห็นอะไรเลย** — เห็นเป็น "ยังไม่มีคนในรอบประเมิน" ทั้งที่ CMO กรอกแล้ว | policy `team_kpi_admin_all` เป็น `FOR ALL … app_role()='admin'` (ไม่มี SELECT ให้ staff) แต่ UI ออกแบบมาว่า "ทุกคนอ่านได้ CMO เขียน" — เจตนาสองฝั่งขัดกัน | ผู้ใช้เข้าใจผิดว่าระบบพัง / ข้อมูลหาย | `team_kpi_reviews`, `src/app/performance-center/team-kpi/page.tsx:55` | เลือกทางเดียว: (ก) DB ถูก → แก้ UI ให้บอกว่าเฉพาะ CMO เห็น, หรือ (ข) UI ถูก → เพิ่ม policy SELECT ให้ staff — **เป็นการตัดสินใจเรื่อง privacy ของคะแนนคน จึงไม่แก้ให้เองโดยพลการ** | probe: staff (Creative Leader) อ่านได้ **0** แถว · admin อ่านได้ **1** | ยังไม่แก้ (รอคุณตัดสิน) |
| P2-02 | P2 | Audit Log | Action สำคัญหลายอย่างไม่ถูกบันทึก | `logAudit()` ถูกเรียกแค่ 5 ไฟล์ | ตรวจย้อนหลังไม่ได้เมื่อเกิดข้อพิพาท | `src/lib/db/*.ts` | เพิ่ม `logAudit()` ที่จุดที่ขาด | ที่มีแล้ว: เปลี่ยนสถานะแคมเปญ, brief, สมาชิก, permissions/approval matrix, อนุมัติ/ตีกลับเบิกงบ, resolve comment · **ที่ขาด: สร้าง/ลบแคมเปญ · Publish (Meta) · Mark Paid · Export ข้อมูล · bulk update · สร้าง/แก้/ลบ task/content/graphic** | ยังไม่แก้ |
| P2-03 | P2 | Permission | `has_module()` **fail-open**: role ที่ไม่มีแถวใน `permissions` จะได้สิทธิ์ระดับ staff อัตโนมัติ | ตั้งใจทำไว้ (มีคอมเมนต์อธิบาย) เพื่อไม่ให้คนทำงานจริงถูกล็อกออก | ถ้ามีใครเพิ่ม role ใหม่แล้วลืมใส่แถว matrix จะได้สิทธิ์เกินเงียบ ๆ | `public.has_module()` | เพิ่ม CI check ว่า `members.role` ทุกค่า ต้องมีแถวใน `permissions` | อ่านจาก function definition | ยังไม่แก้ (เป็น trade-off ที่ตั้งใจ) |
| P2-04 | P2 | Asset / File | ระบบไม่มี Supabase Storage เลย — ไฟล์งานอยู่เป็นลิงก์ Drive/Canva ใน `assets.drive_url` / `canva_url` | ออกแบบมาแบบนั้น | **สิทธิ์เข้าถึงไฟล์จริงอยู่นอกระบบทั้งหมด** — brand scope, expiry, revoke ทำไม่ได้ ใครได้ลิงก์ก็เปิดได้ | `assets` | ถ้าต้องคุมสิทธิ์ไฟล์จริง ต้องย้ายมา Storage + signed URL หรือคุมที่ Drive shared-drive permission | `select * from storage.buckets` → `[]` · ไม่มี `.storage.from(` ในโค้ดเลย | ยังไม่แก้ (เชิงสถาปัตยกรรม) |
| P2-05 | P2 | Frontend | 18 จุดปิด `react-hooks/exhaustive-deps` | ตั้งใจกัน loop | เสี่ยง stale closure — ค่าที่ใช้ใน effect ไม่ตรงกับ state ล่าสุด | 14 ไฟล์ | ทบทวนทีละจุด ใช้ ref หรือ split effect แทน | `grep eslint-disable` | ยังไม่แก้ |
| P3-01 | P3 | DB | `schema_migrations` อ่านได้โดย authenticated ทุกคน รวมคนที่ไม่มีแถวใน `members` | policy `read_migrations` เช็คแค่ `auth.role()='authenticated'` | เปิดเผยชื่อไฟล์ migration (ข้อมูลโครงสร้างระบบ) | `schema_migrations` | เพิ่ม `and app_role() in ('admin','staff')` | probe "ghost user": ทุกตารางได้ 0 **ยกเว้น schema_migrations ได้ 10** | ยังไม่แก้ |
| P3-02 | P3 | Notification | staff/agency คนไหนก็ INSERT `notifications` โดยระบุ `recipient` เป็นใครก็ได้ | `notif_write` เช็คแค่ role | ปลอมแจ้งเตือนในนามระบบได้ (phishing ภายใน) | `notifications` | บังคับ `actor = jwt_member_name()` ใน WITH CHECK | อ่านจาก policy | ยังไม่แก้ |
| P3-03 | P3 | Notification | `notif_read_own` จับคู่ด้วย `split_part(email,'@',1)` ด้วย | ต้องการรองรับหลายรูปแบบชื่อ | ถ้ามีสมาชิกชื่อตรงกับ local-part ของอีเมลคนอื่น จะอ่านแจ้งเตือนข้ามคน | `notifications` | ตัด branch `split_part` ออก ใช้ email/name เต็มพอ | อ่านจาก policy | ยังไม่แก้ |
| P3-04 | P3 | API | `/api/health` ไม่ต้อง auth และใช้ anon client → RLS บล็อกทุก count ทำให้รายงาน `seeded:false` เสมอบน production | ใช้ anon client แต่ตารางปิด RLS หมดแล้ว | ทำให้เข้าใจผิดว่า DB ว่าง (ไม่ leak ข้อมูล) | `src/app/api/health/route.ts` | ใช้ `supabaseAdmin()` + จำกัดให้คืนแค่ boolean | code review | ยังไม่แก้ |
| P3-05 | P3 | Performance | `/my-tasks` 267 kB · `/graphic` 266 kB First Load JS | หน้าใหญ่ component เดียว | โหลดช้าบนมือถือ/3G | build output | code-split drawer/modal ด้วย `dynamic()` | build log | ยังไม่แก้ |
| P3-06 | P3 | Data | 16 tasks ไม่มี `campaign_id` (งานอิสระ) | ตั้งใจได้ (งานทั่วไปไม่ผูกแคมเปญ) | สืบกลับหาแคมเปญต้นทางไม่ได้ | `tasks` | ยืนยันว่าเป็น by design หรือ backfill | integrity query | ต้องยืนยัน |

---

## D. Database & RLS Matrix (ครบทุกตาราง)

**นิยามผู้ใช้ที่ทดสอบ (ตัวจริงจากตาราง `members` 9 คน):**

| ผู้ใช้ทดสอบ | app_role | member_role | brand_access |
|---|---|---|---|
| anon (ไม่ล็อกอิน) | — | — | — |
| ghost (ล็อกอินแต่ไม่มีแถว members) | none | — | — |
| staff-all (`napaporn.m@`) | staff | Senior Graphic Designer | All brands |
| staff-restricted (`orapan.ch@`) | staff | Marketing Manager / BGL | Selected · Teppen · Mainichi |
| agency | agency | — | External only |
| admin (`Linnapat.d@`) | admin | CMO | All brands |
| service_role | — | — | bypass RLS |

**ผลจริงหลังยิง probe (ก่อน apply p14):**

| # | Table | RLS | anon | ghost | staff-all | staff-restricted | agency | admin | Brand scope | Result |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | agency_tasks | ✅ | ✗ | ✗ | RW | RW | RW เฉพาะ `agency_email` ตัวเอง | RW | ✗ (ตาราง 0 แถว) | PASS |
| 2 | app_settings | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a | PASS |
| 3 | **assets** | ✅ | ✗ | ✗ | RW | **RW ทุกแบรนด์** | ✗ | RW | **❌ ไม่มี** | **BROKEN → p14** |
| 4 | audit_log | ✅ | ✗ | ✗ | R + INSERT (บังคับ actor=ตัวเอง) | เท่ากัน | ✗ | เท่ากัน | n/a | PASS (immutable: UPDATE/DELETE = 0 rows) |
| 5 | brands | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a | PASS |
| 6 | budget_items | ✅ | ✗ | ✗ | R | R | ✗ | RW | n/a | PASS |
| 7 | campaign_results | ✅ | ✗ | ✗ | RW | RW เฉพาะแบรนด์ตัวเอง | ✗ | RW | ✅ ผ่าน campaigns | PASS |
| 8 | campaign_types | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a | PASS |
| 9 | campaigns | ✅ | ✗ | ✗ | RW | R/W เฉพาะแบรนด์ตัวเอง (omakase = **0**) · DELETE = admin only · สถานะ Approved = CMO only | ✗ | RW | ✅ | PASS |
| 10 | content_posts | ✅ | ✗ | ✗ | RW | omakase = **0** | ✗ | RW | ✅ | PASS |
| 11 | expense_requests | ✅ | ✗ | ✗ | R ของตัวเอง + Finance View · INSERT บังคับ requester=ตัวเอง + status='Waiting Approval' · UPDATE ต้อง Finance Approve | omakase = **0** | ✗ | RW | ✅ | PASS |
| 12 | expenses | ✅ | ✗ | ✗ | ต้องมี Finance View/Edit | ✅ scope | ✗ | RW | ✅ | PASS (Mark Paid = Co-ordinator only ผ่าน trigger) |
| 13 | graphic_feedback | ✅ | ✗ | ✗ | RW | RW ทุกแบรนด์ | ✗ | RW | ⚠️ ไม่มี (ตาราง 0 แถว) | PARTIAL |
| 14 | graphic_requests | ✅ | ✗ | ✗ | RW | omakase = **0** | R/W เฉพาะ `designer` = ตัวเอง · ห้าม approve/deliver/sign-off (trigger) | RW | ✅ | PASS |
| 15 | kol_channels | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | ไม่มี (1 แถว) | PARTIAL |
| 16 | kol_collaboration_history | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | ไม่มี (0 แถว) | PARTIAL |
| 17 | kol_comments | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | ไม่มี (0 แถว) | PARTIAL |
| 18 | kol_profiles | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | ไม่มี (master list, by design) | PASS |
| 19 | kol_rank_scores | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | ไม่มี (0 แถว) | PASS |
| 20 | kol_rank_weights | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a (config) | PASS |
| 21 | **kols** | ✅ | ✗ | ✗ | RW | **RW ทุกแบรนด์ (omakase 12 แถว · DELETE สำเร็จ)** | ✗ | RW | **❌ ไม่มี** | **BROKEN → p14** |
| 22 | members | ✅ | ✗ | ✗ | R ทั้งหมด · UPDATE เฉพาะแถวตัวเอง | เท่ากัน | R เฉพาะแถวตัวเอง | RW | n/a | PASS (escalation ถูกบล็อกโดย `members_guard`) |
| 23 | notifications | ✅ | ✗ | ✗ | R/UPDATE เฉพาะของตัวเอง · INSERT ได้ทุก recipient | เท่ากัน | เท่ากัน | เท่ากัน | n/a | PARTIAL (P3-02/03) |
| 24 | org_settings | ✅ | ✗ | ✗ | R ทั้งหมด · W เฉพาะ 3 key allow-list | เท่ากัน | ✗ | RW | n/a | PASS |
| 25 | permissions | ✅ | ✗ | ✗ | R | R (UPDATE = 0 rows) | ✗ | RW | n/a | PASS |
| 26 | pnl | ✅ | ✗ | ✗ | R | R | ✗ | RW | n/a | PASS |
| 27 | promotion_summary_items | ✅ | ✗ | ✗ | RW | RW ทุกแบรนด์ | ✗ | RW | ⚠️ ไม่มี (1 แถว) | PARTIAL |
| 28 | requests | ✅ | ✗ | ✗ | RW | ✅ scope | ✗ | RW | ✅ | PASS |
| 29 | schema_migrations | ✅ | ✗ | **R (10 แถว)** | R | R | R | R | n/a | PARTIAL (P3-01) |
| 30 | slack_digest_queue | ✅ | ✗ | ✗ | R only | R only | ✗ | R only | n/a | PASS (เขียนได้เฉพาะ service role) |
| 31 | **tasks** | ✅ | ✗ | ✗ | RW | **RW ทุกแบรนด์ (omakase 64 แถว · UPDATE สำเร็จ)** | ✗ | RW | **❌ ไม่มี** | **BROKEN → p14** |
| 32 | team_kpi_reviews | ✅ | ✗ | ✗ | **✗ (0 แถว)** | ✗ | ✗ | RW | n/a | PARTIAL (P2-01) |
| 33 | workflow_state | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a (global) | PASS |
| 34 | workflow_tasks | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a (0 แถว) | PASS |
| 35 | workload_members | ✅ | ✗ | ✗ | RW | RW | ✗ | RW | n/a (0 แถว) | PASS |

**RLS ผลรวม: 35/35 ตารางเปิด RLS · 0 ตารางเปิดให้ anon · ghost user เห็น 0 ทุกตารางยกเว้น `schema_migrations`**

### Negative test ที่ผ่าน (หลักฐาน)

| # | Negative test | ผลจริง |
|---|---|---|
| N3 | staff แก้ `access` ตัวเองเป็น Admin | ❌ BLOCKED — `members: non-admin may not change role/access/brand/email` |
| N4 | staff ขยาย `brand_access` ตัวเองเป็น All brands | ❌ BLOCKED (ข้อความเดียวกัน) |
| N5 | staff แก้แถว member คนอื่น | ❌ rows=0 (RLS กรองออก) |
| N6 | non-CMO ย้ายแคมเปญเป็น Approved | ❌ BLOCKED — `เฉพาะ CMO เท่านั้นที่ย้ายแคมเปญเข้าสถานะ Approved ได้` |
| N7 | ปลอม `actor_email` ใน audit_log | ❌ BLOCKED — RLS WITH CHECK |
| N8 | แก้แถว audit_log เดิม | ❌ rows=0 (ไม่มี policy UPDATE) |
| N9 | ลบแถว audit_log | ❌ rows=0 (ไม่มี policy DELETE) |
| N10 | staff เขียนทับ `brands_config` | ❌ rows=0 (allow-list ไม่ครอบ key นี้) |
| N11 | staff แก้ permissions matrix | ❌ rows=0 |
| — | anon อ่าน 12 ตารางสำคัญ | ❌ ได้ 0 ทุกตาราง |
| — | ghost user (ไม่มีแถว members) อ่าน 8 ตาราง | ❌ ได้ 0 (ยกเว้น schema_migrations) |
| **N1** | **staff-restricted UPDATE omakase tasks** | ⚠️ **ALLOWED rows=64** ← P1-01 |
| **N2** | **staff-restricted DELETE omakase kols** | ⚠️ **ALLOWED rows=12** ← P1-01 |

### Data integrity (ทุกข้อ = 0 ยกเว้นที่ระบุ)

| ตรวจ | ผล |
|---|---|
| content_posts ที่ `campaign_id` ไม่มีในตาราง campaigns | 0 |
| content_posts ที่ไม่มี campaign_id | 0 |
| graphic_requests campaign_id เสีย / ว่าง | 0 / 0 |
| tasks campaign_id เสีย | 0 |
| tasks ไม่มี campaign_id | **16** (งานอิสระ — ต้องยืนยันว่าตั้งใจ) |
| tasks ไม่มี assignee | 0 |
| kols / expense_requests campaign_id เสีย | 0 / 0 |
| assets ที่ `graphic_request_id` ไม่มีต้นทาง | 0 |
| แคมเปญที่ถูก soft-delete | 0 |
| ลูกกำพร้าใต้แคมเปญที่ถูกลบ | 0 |
| **Duplicate protection** | มี UNIQUE index กัน retry ซ้ำครบ: `tasks/content_posts/graphic_requests/kols _blob_id_uniq` + `content_posts/graphic_requests/kols _source_uniq` + `assets_graphic_request_uniq` + `campaign_results_row_id_key` |
| Primary key | ✅ ครบ 35/35 ตาราง |
| Foreign key | ✅ 22 FK · ทุก brand/campaign column มี FK |
| FK index | ⚠️ FK ที่เป็น `brand` ไม่มี index (ตารางเล็กมาก ผลกระทบ ~0 · p14 เพิ่มให้ 3 ตัว) |

---

## E. Files Changed

| ไฟล์ | สิ่งที่ทำ | เหตุผล | ผลกระทบ | Test |
|---|---|---|---|---|
| `web/supabase/security_p14_brand_scope_work.sql` | **ใหม่** — เปลี่ยน `staff_rw` ของ `tasks` / `kols` / `assets` เป็น policy แยก SELECT/INSERT/UPDATE/DELETE ที่ผ่าน `brand_visible(brand)` + เพิ่ม index `brand` 3 ตัว | ปิด P1-01 | สมาชิกที่จำกัดแบรนด์จะไม่เห็น/แก้ข้อมูลนอกแบรนด์อีก · สมาชิก "All brands" และ CMO ไม่เปลี่ยน | dry-run บน production (rollback แล้ว) — ดูหลักฐานข้อ G |
| `web/supabase/security_p14_brand_scope_work_rollback.sql` | **ใหม่** — คืนเป็น `staff_rw` เดิม | ตามกฎข้อ 6 | คืนสภาพเดิม 100% (index ไม่ถูกลบ ไม่มีผลด้านสิทธิ์) | — |
| `AUDIT_2026-08-01.md` | **ใหม่** — รายงานฉบับนี้ | — | — | — |

**ไม่มีการแก้ไฟล์ TypeScript ใด ๆ** — ปัญหา P1 อยู่ที่ชั้น database ล้วน และแก้ที่ UI ไม่ถือเป็นการแก้ (กฎข้อ "ห้ามใช้การซ่อนเมนูเป็น security control")

**ไม่มีการเปลี่ยนแปลงใด ๆ ที่ถูก commit บน production database** — dry-run ทุกครั้งจบด้วย `ROLLBACK`

### ประตูหนีไฟที่ใส่ไว้ใน p14 (สำคัญ)

- `tasks` SELECT/UPDATE ยอมให้เมื่อ `assignee = jwt_member_name()` → ใครถูกมอบหมายงานข้ามแบรนด์ **ยังเห็นและปิดงานตัวเองได้** ไม่หายจาก My Tasks
- `kols` SELECT/UPDATE ยอมให้เมื่อ `owner = jwt_member_name()` → KOL Specialist ที่ดูแลดีลนั้นยังทำงานได้
- INSERT/DELETE **ไม่มี**ประตูหนีไฟ — สร้าง/ลบได้เฉพาะแบรนด์ในสิทธิ์
- แถวที่ `brand` เป็น NULL: คน "All brands" และ CMO เห็น · คนที่ถูกจำกัดไม่เห็น (fail-closed)
- โค้ดฝั่ง app แปล RLS error เป็นภาษาคนอยู่แล้ว (`src/lib/db/assert.ts` → *"บัญชีของคุณไม่มีสิทธิ์ทำรายการนี้…"*) จึงไม่มีหน้าจอไหนขึ้น error ดิบ

---

## F. Database Migration

**วิธี apply** (Supabase SQL Editor → project `zxxpyknoymdlhckpchse`):

```
web/supabase/security_p14_brand_scope_work.sql
```

- idempotent (`drop policy if exists` ก่อน create ทุกตัว · `create index if not exists`)
- อยู่ใน `begin; … commit;` block เดียว — ล้มกลางทางไม่ทิ้งสถานะครึ่ง ๆ
- บันทึกตัวเองลง `schema_migrations` ผ่าน `record_migration()` ตาม convention เดิม
- ไม่ลบข้อมูลใด ๆ · ไม่แตะ schema/column
- **Rollback:** `web/supabase/security_p14_brand_scope_work_rollback.sql`
- **Backup plan:** ไม่แตะข้อมูล จึงไม่ต้อง restore — แค่รัน rollback ก็คืนสภาพ

หลัง apply แล้วรัน probe นี้ยืนยันได้ทันที:

```sql
begin;
set local role authenticated;
set local request.jwt.claims = '{"role":"authenticated","email":"orapan.ch@teppenthailand.co.th","app_role":"staff","member_role":"Marketing Manager / BGL"}';
select 'omakase tasks' k, count(*) from tasks  where brand='omakase'
union all select 'omakase kols',   count(*) from kols   where brand='omakase'
union all select 'omakase assets', count(*) from assets where brand='omakase'
union all select 'teppen+mainichi tasks (ต้องยังเห็น)', count(*) from tasks where brand in ('teppen','mainichi');
rollback;
-- คาดหวัง: 0 / 0 / 0 / 87
```

---

## G. Test Evidence

| ชั้น | คำสั่ง / วิธี | ผล |
|---|---|---|
| Type check | `npx tsc --noEmit` | ✅ ไม่มี error |
| Lint | `npm run lint` | ✅ `No ESLint warnings or errors` |
| Unit / logic test | `npm test` (30 suite, tsx) | ✅ **1,673 passed · 0 failed** |
| Production build | `npm run build` | ✅ `Compiled successfully` · 30 static page · 12 API route · exit 0 |
| Secret ใน client bundle | ตรวจ static + โค้ด | ✅ ไม่มี client component import `supabaseAdmin` · env ที่ client อ้างถึงมีตัวเดียวคือ `NEXT_PUBLIC_REQUIRE_AUTH` · `import "server-only"` ครอบ `supabaseAdmin/apiAuth/slackBot/serverBrands/slackDirectory` (build จะ error ถ้ามีคนเผลอ import จาก client) · ที่ grep เจอคำว่า `service_role` คือ doc comment ของ supabase-js เท่านั้น |
| Code smell | grep | ✅ `as any` = 0 · `@ts-ignore` = 0 · `@ts-nocheck` = 0 · `dangerouslySetInnerHTML` = 0 · `console.log` = 0 · empty catch = 4 (มีคอมเมนต์อธิบายทุกจุด) · `eslint-disable` = 18 (line-scope ทั้งหมด) |
| HTTP route sweep | curl 27 route | ✅ 200 ทั้ง 26 หน้า · 404 ถูกต้องสำหรับ path มั่ว |
| Dead link | ดึง `href="/…"` ทุกจุดในโค้ดแล้วยิงจริง | ✅ ไม่มี dead link |
| Browser console | Chromium, หน้า `/`, `/campaigns/new`, `/finance`, `/graphic`, `/my-tasks` | ✅ **0 error · 0 warning** (มีแต่ info "Download React DevTools") |
| Network | อ่าน request ทั้งหมด | ✅ ทุก request 200 · ไม่มี failed request (มีแต่ RSC prefetch ที่ถูก abort ตอนเปลี่ยนหน้า = ปกติ) |
| Dev server log | 26 route compile | ✅ 0 error / 0 warning |
| Mobile 375×812 | screenshot + `scrollWidth` | ✅ hamburger ทำงาน · `docW=375 = winW` → ไม่มี horizontal overflow |
| Supabase Security Advisor | `get_advisors(security)` | ⚠️ 0 ERROR · 23 WARN — 22 ตัวเป็น "anon/authenticated เรียก SECURITY DEFINER function ได้" ซึ่ง **ปกติและจำเป็น** (RLS policy เรียก function พวกนี้เอง และ function ทุกตัวอ่าน identity จาก JWT ไม่ใช่จาก argument) · เหลือ 1 ตัวที่ควรทำจริง: **Leaked Password Protection ยังปิดอยู่** → เปิดใน Dashboard → Auth → Password |
| RLS probe | 61 probe (positive + negative, ทุก role) | ดูตาราง D |
| Migration dry-run | apply p14 + probe + `ROLLBACK` บน production | ✅ ดูด้านล่าง |

### ผล dry-run ของ p14 (บน production จริง แล้ว rollback)

| probe | ก่อน | หลัง | ผลที่ต้องการ |
|---|---|---|---|
| staff-restricted อ่าน omakase tasks | 64 | **0** | ✅ |
| staff-restricted อ่าน teppen+mainichi tasks | 87 | **87** | ✅ ไม่กระทบงานตัวเอง |
| staff-restricted อ่าน omakase kols | 12 | **0** | ✅ |
| staff-restricted อ่าน omakase assets | 1 | **0** | ✅ |
| staff-restricted UPDATE omakase tasks | rows=64 | **rows=0** | ✅ |
| staff-restricted DELETE omakase kols | rows=12 | **rows=0** | ✅ |
| staff-restricted INSERT omakase task | — | **BLOCKED (RLS)** | ✅ |
| staff-restricted INSERT teppen task | — | **ALLOWED** | ✅ งานปกติไม่พัง |
| staff-all อ่าน tasks / kols / assets | 151 / 36 / 1 | **152\* / 36 / 1** | ✅ ไม่เปลี่ยน |
| admin (CMO) อ่าน tasks / kols / assets | 151 / 36 / 1 | **152\* / 36 / 1** | ✅ ไม่เปลี่ยน |
| anon อ่าน tasks / kols | 0 / 0 | **0 / 0** | ✅ |

\* 152 = 151 + 1 แถว probe ที่ insert ในทรานแซกชันเดียวกัน (ถูก rollback ทิ้งแล้ว)

---

## H. Remaining Risks

| # | ปัญหา | ทำไมยังไม่แก้ | ผลกระทบ | วิธีแก้ | Owner | Priority |
|---|---|---|---|---|---|---|
| 1 | ~~P1-01~~ | — | — | ✅ **ปิดแล้ว** — apply 1 ส.ค. 2569 06:01 UTC · verify ผ่าน | — | ปิด |
| 2 | Leaked Password Protection ปิดอยู่ | เป็น toggle ใน Supabase Dashboard ไม่ใช่ SQL | รหัสผ่านที่หลุดจาก breach อื่นใช้สมัคร/เปลี่ยนได้ | Dashboard → Authentication → Password → เปิด | คุณ | สูง |
| 3 | P2-01 Team KPI: DB (admin only) กับ UI (ทุกคนอ่าน) ขัดกัน | เป็นการตัดสินใจเรื่อง privacy ของคะแนนประเมินคน ไม่ใช่ bug ที่มีคำตอบเดียว — ผมไม่ควรเลือกแทน | ทีมเปิดหน้าแล้วเห็นว่าง เข้าใจว่าระบบพัง | ตัดสินใจ (ก) หรือ (ข) ตามตาราง C แล้วผมแก้ให้ | คุณ | กลาง |
| 4 | P2-02 Audit log ไม่ครบ | ต้องเพิ่มโค้ดหลายจุด ควรทำเป็นรอบแยกพร้อม test | ตรวจย้อนหลังไม่ได้ในเรื่อง publish / mark paid / export / ลบแคมเปญ | เพิ่ม `logAudit()` ตามลิสต์ | ทีม dev | กลาง |
| 5 | P2-04 ไฟล์งานจริงอยู่นอกระบบ (Drive/Canva link) | เป็นเรื่องสถาปัตยกรรม ไม่ใช่ bug | ใครได้ลิงก์ก็เปิดได้ ไม่มี brand scope / expiry / revoke | ย้ายมา Supabase Storage + signed URL หรือคุมสิทธิ์ที่ Drive | คุณ + IT | กลาง |
| 6 | โมดูล **CRM / Customer Segment / Sales Performance** ไม่มีในระบบนี้ | อยู่คนละแอป (teppen-cdp) | ลำดับ workflow ที่คุณระบุจึงขาดช่วง `CRM Action` และ `Sales Performance` | ตัดสินใจว่าจะรวมเข้ามาหรือเชื่อมด้วย campaign_id ข้ามระบบ | คุณ | กลาง |
| 7 | `budget_items`, `pnl`, `workload_members`, `workflow_tasks`, `agency_tasks`, `campaign_types`, `campaign_results` ว่างเปล่า | เป็นสภาพจริงของข้อมูล | โมดูล Budget/Sales บางส่วนยังไม่มีข้อมูลจริงมาแสดง | ป้อนข้อมูลหรือยืนยันว่าเลิกใช้ (**อย่า drop** — RLS/seed อ้างถึงอยู่) | ทีม | ต่ำ |
| 8 | E2E automated test (Playwright) ยังไม่มีในโปรเจกต์ | โปรเจกต์ใช้ tsx test harness ล้วน ไม่มี browser test framework — การเพิ่มเป็นงานตั้งโครงใหม่ ไม่ใช่ scope ของ audit รอบนี้ | workflow ยาว (สร้างแคมเปญ→อนุมัติ→publish) ไม่มี regression net | ตั้ง Playwright + เขียน happy path 1 เส้น + negative 5 เคส | ทีม dev | กลาง |
| 9 | P2-05 `exhaustive-deps` ปิดไว้ 18 จุด | ต้องรีวิวทีละจุด | stale closure ที่ยังไม่แสดงอาการ | รีวิว/refactor | ทีม dev | ต่ำ |
| 10 | โฟลเดอร์ local ตามหลัง origin/main 125 commits | เป็นสภาพ working tree ของคุณ | เสี่ยงแก้โค้ดเก่าทับ | `git fetch` + rebase/สร้าง worktree ใหม่ก่อนแก้เสมอ | คุณ | ต่ำ |

---

## Module Matrix (26 โมดูลตามที่ระบุ)

| # | Module | Route | UI Component | Table | Roles | Upstream | Downstream | Status |
|---|---|---|---|---|---|---|---|---|
| 1 | Dashboard | `/` (= Campaigns) + `/status` | Sidebar, KpiCard, StatusBadge | campaigns, tasks | ทุก role | — | ทุกโมดูล | PASS (ไม่มีหน้า Dashboard แยก — `/` คือ Campaigns) |
| 2 | Marketing Planner | `/campaigns/new` | Campaign Builder 7 ขั้น | campaigns.data (JSONB) | CMO/MM/ME | — | Content, KOL, Budget, Task | PASS |
| 3 | Campaign Management | `/campaigns`, `/campaigns/[id]` | CampaignDetailView | campaigns | สร้าง=ไม่ใช่ Creative · สถานะ/ลบ=CMO | Planner | ทุกโมดูลปลายน้ำ | PASS |
| 4 | Content Calendar | `/content` | WorkViews, ContentDrawer | content_posts | Content/Creative | campaigns | Caption, Artwork | PASS |
| 5 | Content Production | `/content`, `/my-tasks` | ContentItemForm | content_posts, tasks | Creative | campaigns | Artwork | PASS |
| 6 | Caption | ในหน้า `/content` | ContentItemForm | content_posts.caption, org_settings(caption_templates_config) | Creative Leader มอบหมายเจ้าของ (trigger) | Content | Publish | PASS |
| 7 | Artwork Brief | `/graphic` | GraphicDrawer, TaskGraphicBrief | graphic_requests | Creative/Agency | content_posts | Artwork Board | PASS |
| 8 | Artwork Board | `/graphic/artwork` | — | graphic_requests.data.deliverables | Creative/Agency | Brief | Assets | PASS |
| 9 | KOL Management | `/kol` | KolDrawer, KolItemForm | kols, kol_profiles/channels/collab/rank | KOL Specialist | campaigns | Performance | **BROKEN (brand scope) → p14** |
| 10 | Budget Management | `/finance`, `/api/budget-sheet` | ExpenseTabs | budget_items (**0 แถว**), expense_requests, expenses | Finance/CMO | campaigns | P&L | PARTIAL |
| 11 | Task / My Task | `/my-tasks`, `/team` | WorkViews | tasks | ทุกคน | ทุกโมดูล | — | **BROKEN (brand scope) → p14** |
| 12 | Approval Workflow | กระจายทุกหน้า | — | campaigns(status), expense_requests, graphic_requests | CMO / Finance Approve / Creative Leader | — | Publish | PASS (บังคับที่ trigger + RLS) |
| 13 | Branch Management | ในฟอร์ม | — | brands.branch_list, campaigns.branch | — | — | — | PARTIAL (ไม่มีหน้าจัดการแยก · RLS ไม่มี branch scope) |
| 14 | Brand Management | `/settings` | — | org_settings(brands_config), brands | Admin | — | ทุกโมดูล | PASS |
| 15 | User & Team | `/settings`, `/team`, `/api/members/invite` | RoleSwitcher | members, permissions | Admin | — | ทุกโมดูล | PASS |
| 16 | CRM | — | — | — | — | — | — | **NOT FOUND** (อยู่ที่ teppen-cdp) |
| 17 | Customer Segment | — | — | — | — | — | — | **NOT FOUND** |
| 18 | LINE OA / CRM Automation | `/api/notify` | — | notifications | — | ทุกโมดูล | — | PARTIAL (LINE push อย่างเดียว ไม่มี automation) |
| 19 | Ads Performance | `/ads`, `/api/ad-actuals-sheet` | ImportAdActualsButton | campaigns.data, Google Sheet | MM/CMO | campaigns | Performance Center | PASS |
| 20 | Social Performance | `/platforms`, `/api/performance-sheet` | — | Google Sheet mirror | MM/CMO | content_posts | Performance Center | PASS |
| 21 | Sales Performance | — | — | pnl (**0 แถว**) | — | — | — | **NOT FOUND** |
| 22 | Campaign Performance | `/performance-center`, `/performance-center/creative-kpi` | KpiCard | campaign_results (**0 แถว**) | ทุกคน | ทุกโมดูล | Dashboard | PARTIAL |
| 23 | File & Asset | `/assets` | AssetLinkList | assets (ลิงก์ Drive/Canva) | Creative | graphic_requests | — | **BROKEN (brand scope) → p14** + P2-04 |
| 24 | Notification | NotificationBell, `/api/notify`, `/api/notify/digest` | NotificationBell | notifications, slack_digest_queue | ทุกคน | ทุกโมดูล | LINE/Email/Slack | PASS (มี P3-02/03) |
| 25 | Settings | `/settings` | — | org_settings, members, permissions, app_settings | Admin | — | ทุกโมดูล | PASS |
| 26 | Audit Log | ใน `/settings` | — | audit_log | staff อ่านได้ · แก้/ลบไม่ได้ | ทุกโมดูล | — | PARTIAL (P2-02) |

---

## ตอบตรง 5 คำถามท้ายบรีฟ

1. **ส่วนใดผ่าน** — Auth/session (fail-safe ถูกต้อง ไม่มี CMO fallback), API guard (12/12 route มี auth), การกันสิทธิ์ escalation (members_guard), audit log ที่แก้/ลบไม่ได้, brand scope ของ campaigns/content/graphic/requests/expenses, การกัน duplicate ด้วย UNIQUE index, referential integrity, typecheck/lint/1,673 test/build, ไม่มี console error, mobile responsive, ไม่มี secret รั่วสู่ client
2. **ส่วนใดยังไม่ผ่าน** — Team KPI ที่ UI กับ DB ขัดกัน, audit log ไม่ครบ, ไม่มี E2E test, ไม่มีโมดูล CRM/Segment/Sales
3. **มีข้อมูลหรือสิทธิ์เสี่ยงหลุดไหม** — **เคยมี และเป็นของจริงไม่ใช่ทฤษฎี · ปิดไปแล้ว**: สมาชิก 2 คนที่ถูกจำกัดเป็น Teppen+Mainichi อ่าน/แก้/ลบ tasks 64 แถว, kols 12 แถว (มีค่าตัว KOL), assets 1 แถวของ Omakase ได้ — พิสูจน์แล้วด้วย probe จริง และหลัง apply p14 probe เดิมได้ 0 ทุกช่อง
4. **Workflow ใดขาดการเชื่อมต่อ** — เส้น `Performance → CRM Action → Dashboard Report` ขาด เพราะ CRM/Customer Segment ไม่ได้อยู่ในระบบนี้ · `Budget` เชื่อมโครงสร้างครบแต่ `budget_items` ว่าง · `campaign_results` ว่าง ทำให้ Campaign Performance ยังไม่มีข้อมูลจริงไหลถึง Dashboard
5. **พร้อม production ไหม** — **พร้อม** · P1-01 ปิดแล้วบน production (verify ซ้ำผ่าน) เหลือแค่เปิด Leaked Password Protection ใน Dashboard · ข้อเดียวที่ยังไม่ผ่านตามบรีฟคือ "ต้องมี E2E critical flow test" ซึ่งโปรเจกต์ยังไม่มี framework รองรับ
