import { PerformancePlaceholder } from "@/components/performance/PerformancePlaceholder";

export default function CampaignRoiPerformancePage() {
  return (
    <PerformancePlaceholder
      title="Campaign ROI Performance"
      subtitle="Performance view สำหรับดู Budget, Actual, Revenue, ROAS และกำไรต่อ campaign"
      emoji="📈"
    />
  );
}
